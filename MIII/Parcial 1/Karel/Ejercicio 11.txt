class program {
   void poner(n)
   {
    if(iszero(n))
    {
     pickbeeper();
     }
     else
     {
      iterate (pred(n))
      putbeeper();
      }
      }
    program () {
        // TODO poner codigo aqui
        poner(0);
        turnoff();
    }
}